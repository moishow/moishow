# 피드 도메인 — 시나리오 규정 (Scenario Spec)

> v1 · 2026-07-09 · 담당: **피드 및 모임 도메인** (자매 스펙: [모임 도메인](../모임/README.md))
> 모임 도메인 스펙 v6의 Track B(피드)를 **독립 도메인 스펙으로 분리**한 문서. 검증 이력은 §5 참조.

---

## 1. 도메인 정의 & 진입 모델

- **대표 진입점**: 하단 네비 **피드 탭**. 추가 진입: **모임 홈의 게시판 탭**(화면 표면은 모임 도메인 A3, 게시글 시스템은 본 스펙).
- **피드 도메인 범위**: **게시글 시스템 전체** — 피드 메인 · 게시글 상세(좋아요·댓글·미디어) · 글 작성/편집 · 신고/모더레이션. **DM 제외.**
- **행위자**: `부원(작성·반응)` / `모임장·관리자(공지 작성 권한 + 모더레이션)` / `팔로워(피드 소비)`.

---

## 2. 콘텐츠 소스 & 엔티티

- **피드 소스** `[크리틱2]`:
  - ① **내 모임 게시글** — 공지 · **정모/번개 모집 자동 카드** `[크리틱4]` · 일반 글
  - ② **팔로우한 사람 게시글** — 번개모집 카드 등
- **모집 카드**: 정모/번개 **개설 시 자동 생성**(제목·일시·장소·정원·참여 버튼) — **생성 로직은 모임 도메인(A4·§3), 피드는 노출·소비만.**
- **소유 엔티티(피드)**: `게시글` · `좋아요` `[크리틱3]` · `댓글` · `미디어 첨부` · `신고` · `피드 노출 로직`.

---

## 3. 도메인 경계

- **← 회원 도메인**: **팔로우 그래프·버튼·목록·프로필 소유** — 피드는 팔로우 결과만 소비. 사용자 차단도 회원.
- **← 모임 도메인**: 모임 · 정모/번개 · 멤버십 · 정산 소유. 피드 카드를 탭하면 모임 홈(A3)·이벤트 상세(A4)로 **넘겨준다**. 공지 작성 권한은 모임 A6(역할 권한)을 따른다.
- **⚠ 경계 확인 필요**: 신고 처리 주체(모임 관리자 vs 플랫폼 운영자) ↔ 운영자 · 알림 인프라 ↔ 공용/회원.

---

## 4. 시나리오

태그는 모임 스펙과 공유: `[검증]` 1차 검증 · `[생애]` 생애주기 · `[크리틱2~4]` 완결성 크리틱 편입.

#### F1. 피드 메인 (피드 탭)
- **유저플로우**: 피드 탭 → 카드 목록(§2 소스 혼합 · **좋아요·댓글 수 표시** `[크리틱3]`) → 카드 탭 → 게시글 상세(F2) / 모집 카드면 이벤트 상세(모임 A4) / 프로필·모임로 이동
- `[검증]` **빈 상태**: 모임 0 · 팔로우 0 → 탐색(홈 도메인)·팔로우(회원 도메인) 유도

#### F2. 게시글 상세 · 반응
- **유저플로우**: (피드 카드 · 모임 게시판) → 게시글 상세 → **좋아요** `[크리틱3]` · 댓글 작성 · 미디어 열람
- `[생애]` 내 댓글 수정·삭제

#### F3. 글 작성 · 편집
- **유저플로우(작성 대상 이원화 · 리뷰 2026-07-13)**: 홈 FAB `글쓰기` → **① 모임 글**(어느 모임 게시판 선택 → 부원 작성 → 피드·게시판 동시 노출) / **② 개인 글**(개인 피드 → 팔로워에게 노출) · 텍스트·**미디어(사진) 첨부**
- `[생애]` 내 글 수정·삭제 · **공지 작성은 모임장·관리자만**(권한은 모임 A6)
- **모집 카드는 수동 작성 대상 아님** — 정모/번개 개설 시 자동 생성(모임 도메인)

#### F4. 게시글 신고 · 모더레이션 `[검증]` ⚠경계
- **유저플로우**: (부원) 게시글/댓글 → 신고 → 사유 선택 → 접수 / (관리자) 신고함 → 숨김·삭제·경고
- 신고함 진입은 모임 A6 관리 대시보드에서 — 처리 화면은 본 스펙 소유
- `[검증]` 빈 상태: 신고함 없음

---

## 5. 검증 로그 (이관 이력)

- **2026-07-09**: 모임 스펙 v6 Track B에서 분리 — B1(클럽 게시글) → **F2·F3**, B2(피드 메인) → **F1**, B3(신고) → **F4**.
- 분리 전 확정된 결정 유지: 피드 소스 2종 + 팔로우는 회원 소유 `[크리틱2]` · 좋아요 포함 `[크리틱3]` · 정모/번개 모집 카드 자동 생성 `[크리틱3·4]` · 피드 상세 = 게시글 상세 재사용(F2로 확정).

**시나리오 총계**: F1~F4 = **4 시나리오**. (모임 14 + 피드 4 = 도메인 합계 18)

---

## 6. 다음 단계

1. ~~시나리오 규정(분리)~~ ✅
2. **페이지 목록 재추출 예정** — 시나리오 확정 후 유저플로우에서 페이지를 추출한다(연결 페이지 기록은 제거됨).
3. **화면 구현** (`design/system/screen_v1/{page}.html`, 기존 디자인 토큰 준수 — 피드는 community 표면, 코랄 허용)
4. 애자일 반복 개선
